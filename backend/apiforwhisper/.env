# Google Cloud Speech-to-Text API credentials
# HuggingFace token for PyAnnote diarization
# Note: HF_TOKEN is now configured in config.json instead of here

# Optional: Set to development for debug mode
ENVIRONMENT=production

# FastAPI settings
HOST=0.0.0.0
PORT=8000
